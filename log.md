# Log

Append-only history. Claude adds a line each time it ingests a source, saves an answer, or lints the wiki.
Newest at the bottom.

- (setup) Second brain created. Empty wiki, waiting for the first source in `raw/`.
